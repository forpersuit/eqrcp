# DRM Worker / 生产 API 根路径（无尾斜杠）
# 本地联调: http://127.0.0.1:8787
# 生产构建必须: https://lic.eqt.net.im
# 完整配置说明: docs/admin/IMPORTANT_admin-config.md
VITE_API_BASE=https://lic.eqt.net.im

# 可选：仅本地脚本自测用。浏览器登录框输入的 secret 才写入 sessionStorage。
# 切勿提交真实值；切勿用 VITE_ 前缀暴露 secret。
# ADMIN_SECRET=