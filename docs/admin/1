根据文档与现有实现，管理界面的设计图景可以这样理解。

───

1. 文档位置与定位

管理端的主设计文档不在 docs/payment/ 里，而在：

┌────────────────────────────────┬────────────────────────────────────────────────────────┐
│ 文档                           │ 角色                                                   │
├────────────────────────────────┼────────────────────────────────────────────────────────┤
│ docs/admin-dashboard-design.md │ 管理后台 + 错误审计的正式设计（Svelte、路由、板块）    │
├────────────────────────────────┼────────────────────────────────────────────────────────┤
│ docs/payment/*                 │ 业务与数据模型背景（授权、吊销、邮箱哈希、Paddle、D1） │
├────────────────────────────────┼────────────────────────────────────────────────────────┤
│ 当前运维兜底                   │ paddle-payment.md §4：D1 Console / wrangler CLI        │
└────────────────────────────────┴────────────────────────────────────────────────────────┘

用户 Portal vs 管理后台是两套产品：

┌──────┬─────────────────────────────────┬──────────────────────────────────────────────────┐
│      │ 用户 Portal（已实现）           │ 管理 Admin（待做）                               │
├──────┼─────────────────────────────────┼──────────────────────────────────────────────────┤
│ 路径 │ cloudflare/eqt-website/portal   │ 设计：apps/admin-dashboard 或 Pages admin.eqt.   │
│      │ .html                           │ net.im                                           │
├──────┼─────────────────────────────────┼──────────────────────────────────────────────────┤
│ 技术 │ 单页 HTML + Tailwind + 原生 JS  │ Svelte（与 Chat v2 组件化一致）                  │
├──────┼─────────────────────────────────┼──────────────────────────────────────────────────┤
│ 身份 │ 购买邮箱 + 验证码会话           │ ADMIN_SECRET / Session（X-Admin-Secret）         │
├──────┼─────────────────────────────────┼──────────────────────────────────────────────────┤
│ 能力 │ 查自己的授权、解绑设备、退款    │ 全库检索、手动发码、吊销、错误审计、系统健康     │
├──────┼─────────────────────────────────┼──────────────────────────────────────────────────┤
│ 受众 │ 终端用户                        │ 你自己 / 运维                                    │
└──────┴─────────────────────────────────┴──────────────────────────────────────────────────┘

───

2. 设计原则（第一性）

来自 admin-dashboard-design.md：

1. 对用户零技术暴露
   面向普通用户的 API 绝不返回 D1_ERROR、表名、堆栈等；技术细节只进审计表，供管理端看。

2. D1 实时审计
   业务异常静默写入 system_error_logs，管理端做可视化。

3. 栈复用、零额外服务器
   • 前端：Svelte
   • 后端：已有 eqt-drm-api Worker + D1
   • 部署：Cloudflare Pages（admin.eqt.net.im）

支付文档里的 CLI/D1 Console 是过渡运维手段；正式方向是这个 Svelte 后台，而不是继续依赖 Cloudflare 控制台手改表。

───

3. 管理页面信息架构（文档规划）

文档写「四大核心板块」，正文明确写出的是 三大业务模块（第四块可理解为登录/鉴权壳）：

admin.eqt.net.im (Svelte SPA)
├── 登录 / 鉴权（ADMIN_SECRET 或 Session）
├── 3.1 错误审计中心 (Error Audit Center)
├── 3.2 授权码与订单管控 (License & Order Management)
└── 3.3 发信引擎与 Webhook 监控 (System Health)

3.1 错误审计中心

数据源：system_error_logs

┌───────────────┬─────────────────────────────────────────────────────┐
│ 字段          │ 含义                                                │
├───────────────┼─────────────────────────────────────────────────────┤
│ level         │ ERROR / WARN / CRITICAL                             │
├───────────────┼─────────────────────────────────────────────────────┤
│ category      │ 如 CHECKOUT_SEND_CODE、PADDLE_WEBHOOK、SMTP_CONNECT │
├───────────────┼─────────────────────────────────────────────────────┤
│ error_message │ 完整底层细节                                        │
├───────────────┼─────────────────────────────────────────────────────┤
│ context_json  │ URL、IP、Email 等上下文                             │
├───────────────┼─────────────────────────────────────────────────────┤
│ created_at    │ ISO 时间                                            │
└───────────────┴─────────────────────────────────────────────────────┘

界面能力：

• 倒序分页列表，CRITICAL 高亮
• 行展开看 JSON / 堆栈
• 按 category、关键词过滤
• 归档 / 清空旧日志

对应后端（已实现）：GET /api/v1/admin/error-logs?limit=50 + X-Admin-Secret。

3.2 授权码与订单管控（最复杂）

对照 payment/DRM 数据模型，管理端要覆盖的是 全站运营能力，而不是用户自己的那几条授权。

检索

• Email（服务端对邮箱做 hash 后对 buyer_email_hash）
• license_code
• Paddle transaction_id

操作

┌───────────────┬────────────────────────────────────────────────────────────────────┐
│ 操作          │ 业务含义（payment 文档）                                           │
├───────────────┼────────────────────────────────────────────────────────────────────┤
│ 手动生成/补发 │ 生成 PLUS 永久/年付码，可选发信                                    │
├───────────────┼────────────────────────────────────────────────────────────────────┤
│ 吊销 Revoke   │ licenses.status → revoked；客户端下次 /verify 得 403 并擦本地 .lic │
├───────────────┼────────────────────────────────────────────────────────────────────┤
│ 解绑设备      │ 清 activations 指纹，释放 max_devices 额度                         │
└───────────────┴────────────────────────────────────────────────────────────────────┘

关联表（drm-flow / schema.sql）

• licenses：码、tier、status、到期、Paddle 关联
• activations：设备指纹
• unbind_records：解绑限额历史

设计 API：

• POST /api/v1/admin/generate-license
• POST /api/v1/admin/revoke-license

现状偏差（实现时要注意对齐）：

┌─────────────────────────────┬──────────────────────────────────────────────┐
│ 设计文档                    │ 当前 Worker                                  │
├─────────────────────────────┼──────────────────────────────────────────────┤
│ POST .../generate-license   │ 实际是 POST /api/v1/admin/generate（已实现） │
├─────────────────────────────┼──────────────────────────────────────────────┤
│ POST .../revoke-license     │ 尚未实现                                     │
├─────────────────────────────┼──────────────────────────────────────────────┤
│ 按 email/code/tx 检索、解绑 │ 尚无专用 admin API（portal 只有 user 侧）    │
└─────────────────────────────┴──────────────────────────────────────────────┘

3.3 系统健康（发信 + Webhook）

• SMTP：测 MAIL_SEND_SERVER 465 TLS
• Paddle 履约：最近 Webhook 接收 / 对账状态

这部分和 paddle-payment.md 的运维诊断（签名密钥、failed 重放）是同一类问题，只是从 CLI/脚本迁到管理 UI。

───

4. 安全与后端边界

[Svelte Admin]  --X-Admin-Secret-->  [eqt-drm-api Worker]
                                         |
                                         +--> D1 (licenses / activations / system_error_logs / ...)
                                         +--> SMTP / Paddle 侧车诊断

• 所有 /api/v1/admin/* 必须校验 ADMIN_SECRET，失败统一 401
• 与用户 Portal 的 user_sessions / 邮箱验证码 完全隔离
• 不向公开网站暴露管理入口；建议独立子域 + 不索引

───

5. 建议的 Svelte 页面结构（可直接开工）

文档只定了业务板块，未给组件树；按复杂度与「Portal 已实现、Admin 更重」可这样拆：

apps/admin-dashboard/          # 或 cloudflare/eqt-admin/
  src/
    App.svelte                 # 路由壳 + 鉴权门
    lib/
      api.ts                   # 带 X-Admin-Secret 的 fetch 封装
      auth.ts                  # secret 存 sessionStorage（勿进 git）
    pages/
      Login.svelte             # 输入 Admin Secret
      ErrorAudit.svelte        # 3.1 日志表 + 过滤 + 展开
      Licenses.svelte          # 3.2 检索 + 详情 + 生成/吊销/解绑
      SystemHealth.svelte      # 3.3 SMTP / Webhook 状态
    components/
      LogRow.svelte
      LicenseCard.svelte
      DeviceList.svelte
      GenerateLicenseForm.svelte

建议实现顺序（先薄后厚）：

1. 鉴权 + 错误审计（API 已有）
2. 手动生成授权（/admin/generate 已有）
3. 检索 + 吊销 + 设备解绑（需补后端）
4. 系统健康探针

───

6. 和 payment 文档的关系（一张总图）

用户侧                          运营侧
─────                          ─────
购买页邮箱验证 ──► Paddle ──► Webhook 写 licenses
       │                              │
       ▼                              ▼
  portal.html                   admin Svelte
  (我的码/解绑/退款)             (全库/发码/吊销/审计)
       │                              │
       └──────── eqt-drm-api + D1 ────┘

• Portal：GDPR 友好的「邮箱登录 + 仅自己的授权」。
• Admin：替代 D1 Console/CLI 的「运维控制台」——审计、补单、吊销、健康检查。
• 为什么用 Svelte：管理端状态多（表格、过滤、详情抽屉、危险操作确认），单文件 portal.html 撑不住；Chat v2 已验证 Svelte+Vite 路线。

───

7. 文档缺口（实现前建议对齐）

1. 「四大板块」正文只写了三块——是否还要 反馈列表（eqt-feedback-api / Telegram）或 Dashboard 总览 KPI，文档未写。
2. 路径命名不一致：generate-license vs 已实现的 generate。
3. 检索 / 吊销 / 解绑 / 清空日志 / SMTP 探测 的 请求响应 JSON 未规格化。
4. 鉴权只有 ADMIN_SECRET，没有多管理员、审计谁操作了什么。

───

一句话：管理界面 = 托管在 admin.eqt.net.im 的 Svelte SPA，三大（业务）页——错误审计、授权/订单管控、系统健康——全部走 eqt-drm-api 的 admin 路由 + ADMIN_SECRET；比 Portal 重的是 全库检索 + 发码/吊销/解绑 + 技术日志，而不是用户自助。

如果你下一步要开工，我可以按「先审计 + 发码（API 已有）」或「先补齐 revoke/search 后端」两种路径给一版具体任务拆分和目录脚手架。


疑问:
上面的分析不错, 是否同步到了 admin-dashboard-design.md 中
工程放在 cloudflare/eqt-admin/
文档缺口, 研究梳理后的文档放在 docs/admin 下